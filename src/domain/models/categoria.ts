export class Categorias {
    _id: string | null
    name: string

    constructor(id: string, name: string) {
        this._id = id;
        this.name = name;
    }

    // Métodos para manipular categorias
}